export class PercentOfOption {
    id: number;
    name: string;    
    value: number;

    constructor(id: number, name: string, val: number) {
        this.id = id;
        this.name = name;
        this.value = val;
    }
}